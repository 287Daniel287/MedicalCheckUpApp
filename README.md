# MedicalCheckUpApp
A proyect on console for University


Prototipo
-Orden:
1-Crear struct paciente, Organizar esta en un vector que almacene a todos los pacientes;
    1-Edad
    2-Historia Clinica
    3-Nombres
    4-Motivo de Consulta
    5-Prioridad(1-Urgente,2-Normal)
    6-Estado(Espera o atendido)
2-Crear Array con tamaño maximo definido al inicio!;
3-Funciones del programa
 1-RegistPatient(Paciente paciente);
 2-DeletPatient(int HistClin);
 3-AttendedPatient(Paciente paciente);
 4-ModifyData(int NewAge, string Motivo, int ID);
 5-PrintPatientList();
 6-PrintStats();
 7-SaveData();
 8-LoadData();
